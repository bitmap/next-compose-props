{
  "name": "next-compose-props",
  "version": "1.0.0",
  "description": "Next.js pageProps composer",
  "author": "Cabe Branson",
  "repository": "github:bitmap/next-compose-props",
  "license": "MIT",
  "main": "dist/composeProps.js",
  "types": "dist/composeProps.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dist": "tsc",
    "prepublishOnly": "rm -rf dist && npm run dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^14.14.33",
    "next": "^10.0.8",
    "typescript": "^4.2.3"
  },
  "peerDependencies": {
    "next": "^10.0.0"
  }
}
